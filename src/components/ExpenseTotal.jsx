import React from "react";

const ExpenseTotal = () => {
    return (
        <div className="alert alert-info">
            <span>Spent so far: $800</span>
        </div>
    );
};

export default ExpenseTotal;